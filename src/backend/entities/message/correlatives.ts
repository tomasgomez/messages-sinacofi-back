export type TSN = {
    id: number;
    institutionCode: string;
    areaCode: string;
    createdAt: Date;

    messageId: string;
};

export type LSN = {
    id: number;
    institutionCode: string;
    areaCode: string;
    createdAt: Date;

    messageId: string;
};

export type OSN = {
    id: number;
    institutionCode: string;
    createdAt: Date;

    messageId: string;
};

export type NSE = {
    id: number;
    institutionCode: string;
    areaCode: string;
    createdAt: Date;

    messageId: string;
};

export type NSR = {
    id: number;
    institutionCode: string;
    areaCode: string;
    createdAt: Date;

    messageId: string;
};

export type NSQ = {
    id: number;
    institutionCode: string;
    areaCode: string;
    createdAt: Date;

    messageId: string;
};