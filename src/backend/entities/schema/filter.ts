export interface Filter {
  messageCode ? : string[];
  messageId ? : string[];
  origin ? : string[];
  destination ? : string[];
  count ? : string;
  offset ? : string;
  cuk ? : string;
  action? : string;
}

